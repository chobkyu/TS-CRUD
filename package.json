{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "app.ts",
  "scripts": {
    "server" : "cd server && nodemon app",
    "client": " cd client && npm start",
    "start": "concurrently --kill-others-on-fail \"cd server && nodemon app\" \"cd client && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.7.22",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "concurrently": "^7.4.0",
    "express": "^4.18.1"
  }
}
